import AppProcessingPage from "../shared/components/AppProcessingPage";

const PageLoading = () => {
  return (
    <AppProcessingPage
      title="Loading..."
      description="We are loading the page. Please wait..."
    />
  );
};

export default PageLoading;
